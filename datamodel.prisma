type Hero {
  id: ID! @unique
  name: String! @unique
  teams: [Team!]!
  power: Number!
  alive: Boolean! @default(value: "true")
}

type Team {
  id: ID! @unique
  name: String! @unique
  leader: Hero
}